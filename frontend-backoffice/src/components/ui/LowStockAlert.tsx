/**
 * LowStockAlert is disabled — product-service has no low-stock endpoint.
 * Will be re-enabled when the backoffice-service exposes a low-stock API.
 */
export default function LowStockAlert() {
  return null;
}