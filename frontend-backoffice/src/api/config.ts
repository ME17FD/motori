/**
 * API Base URL Configuration
 * Points to the API Gateway endpoint.
 * Override via VITE_API_BASE_URL environment variable for different environments.
 */

const API_BASE_URL: string = "http://localhost:8080";

export default API_BASE_URL;