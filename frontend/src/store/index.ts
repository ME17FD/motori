/**
 * Client-side global stores (Zustand). Cart remain in `hooks/useCart` + `utils/cartStorage`.
 */

export { useVehicleStore } from "./vehicleStore";
export type { VehicleStoreState } from "./vehicleStore";
